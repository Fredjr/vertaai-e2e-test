openapi: 3.0.0
info:
  title: E2E Test API
  version: 2.0.0
  description: Updated API with new endpoints (TRIGGER OpenAPI ↔ Code comparator)

paths:
  /api/users:
    get:
      summary: Get all users
      operationId: getUsers
      responses:
        '200':
          description: List of users

  # NEW ENDPOINT - Not implemented in code yet!
  /api/users/{id}/profile:
    get:
      summary: Get user profile (NEW - triggers OpenAPI→Code mismatch)
      operationId: getUserProfile
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: User profile
